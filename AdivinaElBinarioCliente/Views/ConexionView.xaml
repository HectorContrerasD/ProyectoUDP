<UserControl x:Class="AdivinaElBinarioCliente.Views.ConexionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AdivinaElBinarioCliente.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Background="#2C3E50">
        <TextBlock Padding="10" FontSize="35" Foreground="White">Escribe la dirección IP del servidor:</TextBlock>
        <TextBox Margin="20" FontSize="25" Text="{Binding IP}" Background="#34495E" Foreground="White"></TextBox>
        <Button Margin="20" FontSize="25" Command="{Binding ConectarCommand}" Background="#3498DB" Foreground="White">Conectar</Button>
    </StackPanel>


</UserControl>
